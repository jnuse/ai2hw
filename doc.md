
# 职场智能助手开发框架文档

## 1. 概述
本项目旨在为职场用户提供一个基于微信小程序的智能问答平台。通过结合前端界面设计与后端服务交互，实现用户问题的智能响应和历史记录存储功能。

---

## 2. 技术栈
- **前端**：Vue.js、UniApp、WXML、WXSS、JavaScript
- **后端**：Flask、WebSocket、HTTP请求
- **数据库**：MySQL
- **工具**：Webpack（模块打包）、Navicat（数据库管理）

---

## 3. 总体框架

### 3.1 前端架构
- **界面搭建**：
  - 使用 Vue.js 和 UniApp 构建基础框架。
  - 利用 WXML 和 WXSS 进行样式设计，完成页面布局和视觉效果。
- **逻辑实现**：
  - JavaScript 编写页面交互逻辑。
  - 使用 `wx.request` 发起 HTTP 请求，与后端服务进行数据交互。
- **模块打包**：
  - 使用 Webpack 打包前端代码，生成可在微信小程序端运行的模块。

### 3.2 后端架构
- **API 开发**：
  - 基于 Flask 框架编写 RESTful API。
  - 提供接口支持用户登录、问题查询、历史记录存储等功能。
- **服务交互**：
  - 使用 WebSocket 实现实时通信。
  - 处理前端发起的 HTTP 请求，并调用大模型 API 进行智能问答。
- **数据预处理**：
  - 对返回的数据进行清洗和格式化处理，确保数据一致性。

### 3.3 数据库设计
- **MySQL 数据库**：
  - 存储用户信息、问答历史记录等数据。
  - 表结构设计：
    - `historytextlist`：存储用户的问答记录，包括问题内容、答案、时间戳等字段。
  - 数据库连接：
    - 使用 PyMySQL 库实现 Python 程序与 MySQL 的交互。

---

## 4. 功能模块

### 4.1 用户模块
- 用户登录：
  - 微信小程序通过 `code` 获取用户 OpenID。
  - 验证用户身份并生成会话。

### 4.2 智能问答模块
- 输入问题：
  - 用户在小程序端输入问题或选择示例问题。
- 调用 API：
  - 后端接收问题，调用大模型 API 获取答案。
- 返回结果：
  - 将答案返回前端并展示给用户。

### 4.3 历史记录模块
- 记录存储：
  - 每次问答记录存储到 MySQL 数据库中。
- 历史查询：
  - 支持按日期查询历史记录。

---

## 5. 部署流程
1. **前端部署**：
   - 使用 Webpack 打包前端代码。
   - 在微信开发者工具中上传小程序代码。
2. **后端部署**：
   - 部署 Flask 应用到服务器。
   - 配置生产环境的 WSGI 服务器。
3. **数据库部署**：
   - 安装 MySQL 数据库。
   - 导入初始化脚本，创建表结构。
